import React from 'react';
import { useParams } from 'react-router-dom';

const MyItem = () => {
    const {id} = useParams();
    return (
        <div>
            
        </div>
    );
};

export default MyItem;